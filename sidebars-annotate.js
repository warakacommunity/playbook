// @ts-check
// Sidebar for the AfriAnnotate docs (second docs instance, served at
// /annotate). Autogenerated from the annotate/ folder — ordering comes
// from each _category_.json `position` plus per-file `sidebar_position`
// frontmatter, exactly like the main playbook sidebar.
//
// The annotate/ tree is generated by scripts/sync-annotate-docs.mjs; do
// not hand-edit it. See that script's header for how to refresh it.

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  annotate: [{ type: "autogenerated", dirName: "." }],
};

export default sidebars;
